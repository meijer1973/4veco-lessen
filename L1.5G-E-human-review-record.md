# L1.5G-E Human Review Record

Date: 2026-05-18
Review source: user-provided formal human review response
Initial verdict: REVISE
Focused re-review verdict: REVISE
Current implementation response: second blocker addressed; final focused
recheck required

## Verdict

L1.5G-E should not close on the original reviewed build. Technical QA was
green, but the human review found closure-blocking student-experience and
teacher-learning-quality issues in `grafiekenspel`.

## Blocking Findings From Review

1. Answer placeholders revealed expected answers.
   - Numeric inputs used `challenge.expected_answer.value`, for example
     `Bijvoorbeeld 180`.
   - Percentage inputs used the expected percentage value.
2. Percentage-change dropdowns preselected old/new values.
   - This could silently select the wrong comparison for a task.
3. The task screen needed a visible pre-answer checklist.
   - The subtitle gave the general route, but the form needed local
     scaffolding.
4. Wrong-answer feedback was formative but too generic.
   - It did not distinguish wrong graph value, wrong old/new pair, or
     calculation error.
5. `1.1.2-quality-ref.yaml` did not yet include `grafiekenspel` under companion
   games.

## Implementation Response

Platform source was updated after the review:

- `engines/graphical-ui.js`
  - replaces expected-answer placeholders with neutral placeholders;
  - starts percentage old/new selectors with explicit placeholder choices;
  - removes automatic old/new preselection;
  - adds a visible checklist before the answer form;
  - adds diagnostic wrong-answer feedback.
- `engines/graphical.css`
  - styles the checklist and diagnostic feedback.
- `engines/tests/graphical-ui.test.js`
  - adds source-level safeguards against expected-answer placeholders;
  - verifies explicit old/new selection remains required.

Generated lesson output was rebuilt through platform `deploy.js`.

Focused evidence after regeneration:

- focused graphical/adaptive Jest passed: 4 suites, 22 tests;
- `1.1.2` and `1.1.1` complete student-web validations passed;
- Book 1 health passed 26/26;
- procedure-contract validation passed 289 checks;
- screenshot evidence was refreshed for first challenge, percentage selectors,
  and diagnostic feedback states.

## Review State After Implementation

The original human verdict remains recorded as REVISE. The implementation
addresses the listed blocking fixes, but L1.5G-E should still receive a focused
recheck before final closure.

Expected final closure after recheck: PASS WITH FLAGS.

Remaining flag:

- L1.6 still has to prove the graphical game and adaptive-input seam on a fresh
  paragraph.

## Focused Re-Review Finding

The focused re-review confirmed that the original placeholder, old/new
selection, checklist, feedback, and quality-ref blockers were addressed. It
found one new closure-blocking issue:

- after the final challenge was answered, `render()` showed the summary
  immediately because `engine.isComplete()` became true before final feedback
  could render;
- this skipped the feedback for `Indexpunten zijn nog geen procenten`, the most
  important misconception task in the MVP.

## Second Implementation Response

Platform source was updated after the focused re-review:

- `engines/graphical-ui.js`
  - now renders the summary only when the engine is complete and no
    `lastResult` feedback is pending;
  - keeps the final-challenge button label `Bekijk resultaat`, so the student
    reads final feedback first and then opens the summary.
- `engines/tests/graphical-ui.test.js`
  - adds a safeguard that the final-feedback path cannot regress to the old
    `if (engine.isComplete())` short-circuit.

Generated lesson output was rebuilt through platform `deploy.js`.

Focused rendered evidence after regeneration:

- `graphical-final-feedback-desktop-light.png`;
- `graphical-final-feedback-mobile-390x1280-light.png`.

The screenshot script verified this flow:

1. final wrong answer shows feedback and diagnostic text;
2. no summary is shown yet;
3. the button says `Bekijk resultaat`;
4. after clicking that button, the summary appears.

Current state: ready for final focused human recheck, not closed.
