# 2.1.4-review.md - repository sprint disposition

## Verdict

PASS.

This mixed-practice paragraph has been integrated into the repository through
sprint `B2-2.1.4-HARDEN`. The latest root golden package was imported into the
canonical paragraph folder with canonical filenames, rendered output was
rebuilt from the repository folder, and the official consolidation shape remains
`opgaven` plus `antwoorden`.

Lead review round 1 and round 2 both returned `PASS WITH FLAGS` with no
blocking findings.

Sprint `MIXED-OPGAVEN-TARGET-STANDARD-1` created the reusable mixed-opgaven
target standard and audited 2.1.4 against it. The section is accepted as the
intended target form for a mixed-exercise section after adding compact
answer-construction guidance to the student source. The upstream target record
is no longer placeholder-backed: it now uses a non-placeholder SmoothBox mixed
transfer target and status `reviewed_final`.

## Repository checks and corrections

- The sprint preserves the v5 `gemengde_opgaven` contract: no new theory and
  no target-source mutation.
- The imported exercise route consolidates `2.1.1` through `2.1.3`: costs,
  average costs, revenue, profit, break-even, graph interpretation, and
  marginal table reasoning.
- Subquestions use uppercase labels.
- Source information and questions are separated so students must choose the
  correct operation instead of only following a named formula.
- Answer models include formula, substitution, result, unit, and economic
  interpretation.
- The paragraph builder was adjusted to emit only the official consolidation
  print outputs: `opgaven` and `antwoorden`.
- The upstream package paragraaf is treated as source/strategy evidence, not as
  a new official theory paragraph.
- The added answer scaffold names calculation, table, graph, and explanation
  answer routes for context-heavy mixed questions.

## Carry-forward notes

- Chapter 2.1 still carries the non-blocking PDF-size warning during validation.
