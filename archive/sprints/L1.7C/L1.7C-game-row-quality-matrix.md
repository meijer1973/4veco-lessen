# L1.7C Game-Row Quality Matrix

Date: 2026-05-23
Status: CLOSED PASS WITH FLAGS

## Summary

The three second-row practice families now consume a shared scoped skill-map
route panel instead of exposing separate or unfiltered skill-tree views. The
first human review returned REVISE because the route panel could expose internal
skill IDs in the focus line. Focused human recheck accepted the fix and L1.7C
closed as PASS WITH FLAGS.

| Game family | Current disposition | Evidence | Flags |
|---|---|---|---|
| `Redeneren` | PASS WITH FLAGS for controlled pilot review | Generated shells load `skill-map-engine.js`, `skilltree/base-elements.js`, paragraph skill-tree data, and `skill-map-route-ui.js`; sidebar renders `Oefenroute Redeneren`; visible progress language is softened to practice progress. | Needs human review for reasoning depth and replay value; current modes still lean on predefined CSV structures. |
| `Rekenen` / `Stappenplan` | PASS WITH FLAGS for current procedure-practice role | Generated shells load the shared route stack; sidebar renders `Oefenroute Rekenen`; procedure contract remains green; result language is softened to `Alles goed`. | This is still a procedure/stappenplan practice engine, not a full numeric calculation engine. Later scaling should decide when explicit calculation field, answer field, and unit handling are required. |
| `Grafieken` | PASS WITH FLAGS for controlled pilot review | Generated shells load the shared route stack; work panel renders `Oefenroute Grafieken`; §1.1.3 `Broodjesverkoop` line chart now hides direct point-value labels and uses y-axis ticks. | Still an MVP. Later graph-heavy scaling needs more less-labelled variants and independent axis/grid reading. |

## Shared Route Criteria

| Criterion | Technical state |
|---|---|
| Aspect-scoped route | PASS: route requests use `reasoning`, `calculation`, and `graphical` filters. |
| Full catalog hidden by default | PASS: game route panels request compact mode with `allowFullView: false`. |
| One shared route renderer | PASS: `skill-map-route-ui.js` is loaded by all three game shells. |
| Student primary action clear | PASS WITH FLAGS: route panel shows `Start oefenen` or `Ga verder`; game screens still retain their own start buttons and need UX review. |
| Student-facing focus label | PASS AFTER TARGETED FIX: recommended focus text now uses the visible skill label and has a regression test proving `A61` is not visible. |
| Product-use boundaries | PASS: no adaptive, diagnostic, mastery, sequencing, summative, AI, PV projection, or PV machine-promotion authorization. |
| Mobile/dark/keyboard | PASS WITH FLAGS: post-revision route screenshots exist for desktop/mobile evidence; this is still sprint evidence, not a reusable automated game screenshot gate. |

## Fix / Carry / Defer Classification

Fixed after first human review:

- route panel no longer exposes internal recommended skill IDs such as `A61`;
- `1.1.1` landing copy now describes only `redeneren` and `rekenen`, because no
  graph route is present there.

Fix before closure if focused human review finds it:

- route panel overcrowding on mobile;
- route panel suggesting automatic sequencing or assessment;
- graph route still too answer-revealing after the less-labelled variant.

Carry as explicit flags:

- graphical game remains scaffolded MVP;
- reasoning game needs richer future variants;
- procedure game is not yet a full numeric calculation engine.

Defer:

- exit-ticket implementation;
- landing-page information architecture;
- adaptive behavior, diagnostics, mastery, sequencing, student-facing AI,
  summative use, PV projection, and PV machine promotion.
