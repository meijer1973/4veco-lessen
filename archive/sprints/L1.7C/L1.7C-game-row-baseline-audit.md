# L1.7C Game-Row Baseline Audit

Date: 2026-05-23
Status: ACTIVE; BASELINE STARTED

## GAME-UX-1 Intake

Platform dependency accepted:

- Commit: `6509895`
- Tag: `checkpoint/GAME-UX-1-shared-skill-map-engine`
- Result report: `../4veco-platform/reports/sprints/GAME-UX-1-result.md`

GAME-UX-1 implemented the runtime contract but generated no lesson output. The
lesson repo must therefore deploy/regenerate through the platform workflow
before L1.7C can judge student-facing game-row behavior against the new shared
skill-map route model.

## Pre-Deploy Observations

Before L1.7C deploy/regeneration:

- lesson `shared/` contains old skill-tree runtime files but no
  `shared/skill-map-engine.js`;
- existing paragraph landing pages already show the three second-row routes
  `Redeneren`, `Rekenen`, and `Grafieken`;
- existing `wiskundevaardigheden.html` skill-tree copy still includes
  `beheerst`, which conflicts with the new non-mastery practice-progress
  language;
- existing `instapquiz` pages still contain `score` and `mastery-dashboard`
  identifiers; these are not the three-aspect game row, but they remain a
  boundary watch item for L1.7B/exit-ticket and L1.7D landing cleanup.

## Post-Deploy Observations

After L1.7C platform work and Book 1 deploy:

- lesson `shared/` contains `skill-map-engine.js`,
  `skill-map-route-ui.js`, and `skill-map-route.css`;
- `Redeneren`, `Stappenplan/Rekenen`, and `Grafieken` generated shells load
  the shared route stack:
  - `skill-map-engine.js`;
  - `skilltree/base-elements.js`;
  - `skilltree/<paragraph>.js`;
  - `skill-map-route-ui.js`;
- each game family renders a compact route panel:
  - `Oefenroute Redeneren`;
  - `Oefenroute Rekenen`;
  - `Oefenroute Grafieken`;
- game route panels request compact mode and do not expose the full catalog by
  default;
- §1.1.3 `Broodjesverkoop` now has a less-labelled line-chart variant:
  direct point-value labels are hidden and y-axis ticks are present;
- complete student-web validators pass for `1.1.1`, `1.1.2`, and `1.1.3`;
- procedure-contract validation remains green with 341 checks.

## Current Audit Disposition

The game row is ready for human review, not closure.

See:

- `L1.7C-game-row-quality-matrix.md`
- `L1.7C-technical-qa-report.md`
- `L1.7C-human-review-packet.md`

Known flags to review:

- `Redeneren` still needs judgment on reasoning depth and replay value;
- `Rekenen` is currently the procedure/stappenplan practice engine, not a full
  numeric calculation-input engine;
- `Grafieken` has one less-labelled variant but remains scaffolded MVP;
- browser/screenshot QA has not yet been recorded for the new route panels.
