# L1.7C-MATH Student Experience Review

Date: 2026-05-26
Status: FIRST REVIEW RECORDED; REVISE ADDRESSED; FOCUSED RECHECK REQUIRED

## Verdict

First-review verdict: **REVISE**

Focused revision state: targeted fix implemented; ready for focused recheck.

## Review Summary

The primary route restoration is structurally correct:

- `1.1.2` primary `Rekenen` opens the old skill-tree math game
  (`wiskundevaardigheden.html`);
- `1.1.3` primary `Rekenen` also opens the skill-tree math game while
  `Grafieken` remains a separate route;
- `stappenplan.html` remains available as `Rekenstappen` support;
- `1.1.1` does not promote its unscoped/full-catalog skill tree to primary
  practice and keeps it collapsed as `Brede vaardigheidskaart`.

The first review found one closure blocker:

> The restored math-game result state could show visible internal IDs, for
> example `Volgende: A39`.

That is student-facing copy inside the primary math route and violates the
stable companion specification.

## Targeted Fix Evidence

After the focused revision:

- result next-action copy uses the skill label via `skillDisplayName(next.id)`;
- screenshot QA records `Volgende: Prijsindex (CPI) berekenen`;
- screenshot QA asserts no internal `A##` or `B##` code appears in result text;
- dependency graph node captions now show `Vaardigheid` instead of raw IDs.

## Focused Recheck Questions

1. Does the post-exercise result state avoid `Volgende: A39` or similar
   internal-code leakage?
2. Does the result next action use a readable skill label?
3. Are dependency node captions free from visible internal skill IDs?
4. Does the restored math route still feel clear after the targeted fix?

## Closure State

Do not close from this first review record alone. L1.7C-MATH needs focused
human recheck and lead-review summary before closure.
