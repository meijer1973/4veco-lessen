# L1.7C-MATH Student Experience Review

Date: 2026-05-26
Status: REVIEW COMPLETE; PASS WITH FLAGS

## Verdict

Student-experience verdict: **PASS WITH FLAGS**

The first review returned **REVISE** because the restored math-game result state
could show internal IDs such as `Volgende: A39`. The focused recheck accepts the
targeted fix: result and dependency states now use student-facing labels rather
than visible internal skill IDs.

## Review Answers

| Question | Answer |
| --- | --- |
| On `1.1.2`, is primary `Rekenen` clearly the old skill-tree math game? | **Yes.** The primary `Rekenen` card opens `wiskundevaardigheden.html`, while `Rekenstappen` remains separate support. |
| On `1.1.3`, is primary `Rekenen` the skill-tree math game while `Grafieken` remains separate? | **Yes.** `Redeneren`, `Rekenen`, and `Grafieken` are separate oefenroutes. |
| Is `Rekenstappen` findable as support without replacing the math game? | **Yes.** It sits in the support area, not as the primary math route. |
| Does `1.1.1` avoid pushing an unscoped/full-catalog skill tree as default practice? | **Yes.** The unscoped skill tree remains collapsed as `Brede vaardigheidskaart`. |
| Does the student see a coherent row of practice choices? | **Yes.** The old math game is no longer hidden behind the step-based support route. |
| Do screenshots support readable layout? | **Yes.** Local evidence contains 17 screenshots, including the post-result skill-tree state. |
| Are visible labels free from internal IDs such as `A38`, `A39`, `A61`, `A62`, or `A63`? | **Yes for the checked route, result, and dependency states.** |
| Is the post-exercise next action readable as a skill label? | **Yes.** The observed result copy is `Volgende: Prijsindex (CPI) berekenen`. |
| Are dependency-view internal IDs kept out of visible node labels? | **Yes.** Dependency captions use `Vaardigheid` and readable skill names rather than raw IDs. |

## Carry-Forward Flags

- Skill-tree progress language still uses stars and `route geoefend` style copy;
  Scale Gate 1 should confirm this remains practice-progress language and does
  not imply mastery.
- The restored math game is practice, not target-exercise-readiness evidence.
- Keyboard/focus-order proof for the restored math game should be strengthened
  before scale.

## Boundary Confirmation

This review does not authorize diagnostics, mastery decisions, automatic
sequencing, grading or summative use, student-facing AI, PV projection, PV
machine promotion, Scale Gate 1, or broad companion scaling.
