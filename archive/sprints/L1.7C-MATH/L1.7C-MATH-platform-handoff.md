# L1.7C-MATH Platform Handoff Draft

Date: 2026-05-26
Status: DRAFT; NOT SENT; IMPLEMENTATION NOT STARTED

## Request

Support lesson sprint L1.7C-MATH by restoring the old skill-tree math game as
the primary `Rekenen` practice route while preserving `stappenplan` as support.

## Platform Surfaces Likely Affected

- `build-scripts/platform/build-landing-page.js`
- `build-scripts/platform/build-skilltree-shells.js` if the math game shell
  needs safer default route behavior
- `engines/skilltree-engine.js`
- `engines/skilltree-ui.js`
- `engines/skill-map-engine.js`
- landing-page and game-row tests

## Required Generator Behavior

When both `wiskundevaardigheden.html` and `stappenplan.html` exist:

- primary `Rekenen` route must point to `wiskundevaardigheden.html`;
- `stappenplan.html` must remain visible as support, step practice, or
  `Extra steun`;
- the landing page must not describe `stappenplan` as the replacement math
  game;
- `wiskundevaardigheden.html` must not be duplicated as only an advanced
  `Volledige vaardigheidskaart` source item if it is already the primary math
  route.

## Required Four-Part Review

Platform and lesson evidence must show how these parts fit together:

1. skill-tree math game;
2. reasoning game;
3. graph game;
4. shared skill-map / route-display layer.

## Product Boundaries

Do not authorize diagnostics, mastery decisions, automatic sequencing, grading,
summative use, student-facing AI, PV projection, PV machine promotion, CP-6
closure, Year-1 closure, or broad companion scaling.

## Proof Expected Back From Platform

- focused tests proving route priority and support-route retention;
- generated lesson output through platform scripts only;
- deploy/link/data checks;
- relevant paragraph student-web validation;
- screenshot evidence for the restored primary math route and retained
  `stappenplan` support route;
- explicit statement whether skill-tree default display is scoped enough for
  student use or still a carried overload flag.
