# L1.7C-MATH Teacher Learning Quality Review

Date: 2026-05-26
Status: FIRST REVIEW RECORDED; REVISE ADDRESSED; FOCUSED RECHECK REQUIRED

## Verdict

First-review verdict: **REVISE**

Focused revision state: targeted fix implemented; ready for focused recheck.

## Review Summary

The route architecture is mostly restored:

- scoped skill-tree math pages are primary `Rekenen` for `1.1.2` and `1.1.3`;
- `stappenplan` remains procedure support;
- unscoped `1.1.1` is not promoted to primary math practice;
- reasoning and graph routes remain separate practice engines;
- the shared route/skill-map language remains present in the game row.

The first review found a core-spec blocker:

> The restored math game could render internal skill IDs in visible result copy,
> for example `Volgende: A39`.

This issue is inside the primary math route and therefore cannot be carried as
an ordinary flag.

## Targeted Fix Evidence

The targeted revision changes `skilltree-ui.js` so result next-action copy uses
student-facing skill names instead of internal IDs.

Additional cleanup:

- dependency graph node captions no longer render raw `A##` IDs;
- visible failure fallback no longer appends a raw skill ID;
- focused tests assert that result next-step copy uses `skillDisplayName`;
- screenshot QA now exercises the math game to a result state and checks for
  internal-code leakage.

## Remaining Teacher-Learning Questions

1. Is the visible-label requirement now satisfied in the math-game result state?
2. Is the dependency view now acceptable without raw internal IDs?
3. Are any remaining flags outside the core three-game-plus-shared-layer
   architecture?
4. May Scale Gate 1 consider the game-row architecture restored after focused
   recheck, or is another product revision required?

## Closure State

Do not close from this first review record alone. L1.7C-MATH needs focused
human recheck and lead-review summary before closure.
