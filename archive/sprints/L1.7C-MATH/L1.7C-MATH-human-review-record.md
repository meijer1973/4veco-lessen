# L1.7C-MATH Human Review Record

Date: 2026-05-26
Status: FIRST REVIEW RECORDED; REVISE ADDRESSED; FOCUSED RECHECK READY; NOT CLOSED

## Review Inputs

Reviewed:

- L1.7C-MATH human review packet;
- stable companion core specification;
- sprint plan, history audit, technical QA, validation log and screenshots;
- generated `1.1.1`, `1.1.2`, and `1.1.3` landing pages;
- generated `wiskundevaardigheden`, `stappenplan`, reasoning, and graph-game
  pages;
- platform `build-landing-page.js`, `skilltree-ui.js`,
  `skill-map-engine.js`, `skill-map-route-ui.js`, reasoning and graphical
  runtime evidence.

## First Review Verdicts

- Student-experience verdict: **REVISE**
- Teacher-learning-quality verdict: **REVISE**
- Lead-review verdict: **REVISE**

## Accepted Findings

The review accepted that:

- `1.1.2` and `1.1.3` restore primary `Rekenen` to
  `wiskundevaardigheden.html`;
- `stappenplan.html` remains visible as `Rekenstappen` support;
- `1.1.1` correctly avoids promoting its unscoped/full-catalog skill tree to
  primary practice;
- technical evidence is strong.

## Closure Blocker

`L1.7C-MATH-B1`:

The restored math-game result state could expose internal skill IDs in visible
student-facing copy, for example `Volgende: A39`.

This violates the stable companion specification and blocks closure.

## Targeted Revision

Implemented after the first review:

- result next-action copy now uses `skillDisplayName(next.id)`;
- observed post-result copy is `Volgende: Prijsindex (CPI) berekenen`;
- dependency graph node captions now render `Vaardigheid`, not raw `A##`;
- focused source regression tests were added;
- screenshot QA now drives the math game to a result state and fails on visible
  internal `A##`/`B##` codes;
- lesson output was regenerated through platform deploy only.

## Boundary Confirmation

L1.7C-MATH does not authorize:

- diagnostics;
- mastery decisions;
- automatic sequencing;
- grading or summative use;
- student-facing AI;
- PV projection;
- PV machine promotion;
- Scale Gate 1;
- broad companion scaling.

## Current State

Focused recheck is required. No closure log should be created until the
focused recheck returns a closure verdict and the lead-review summary is
updated.
