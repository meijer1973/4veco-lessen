# L1.7B-MAP Technical QA Report

Date: 2026-05-26
Status: TECHNICAL QA GREEN; READY FOR HUMAN REVIEW

## Scope

L1.7B-MAP resolves the `1.1.1` checkpoint metadata mismatch from
`A43/A04` versus `B01/B02`.

The sprint does not upgrade the checkpoint into target-exercise-readiness
evidence. It makes the metadata honest:

- checkpoint-assessed skills: `B01`, `B02`;
- target-exercise skill set: `A43`, `B01`, `B02`;
- target-readiness evidence: `false`.

## Platform Changes

Changed platform source:

- `source-data/book-1/exit-ticket/1.1.1.json`
- `engines/exit-ticket-engine.js`
- `engines/tests/exit-ticket-engine.test.js`
- `engines/tests/exit-ticket-metadata-alignment.test.js`

Implemented behavior:

- `1.1.1` checkpoint target/scope metadata now uses `B01/B02`.
- `A04` is removed from checkpoint target/scope metadata.
- `metadataAlignment` records paragraph skills, target-exercise skills, source
  refs, and `targetReadinessEvidence: false`.
- engine validation now requires metadata alignment fields.
- engine validation rejects target-readiness status unless the checkpoint
  covers all target-exercise skill IDs.
- focused tests assert the exact `1.1.1` metadata route and prevent silent
  regression.

## Generated Lesson Output

Generated through:

```powershell
node scripts/deploy.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"
```

Changed lesson output:

- `Boek 1 - Grondslagen, vraag en aanbod/shared/exit-ticket-engine.js`
- `Boek 1 - Grondslagen, vraag en aanbod/shared/exit-ticket/1.1.1.js`

No generated lesson output was hand-patched.

The `1.1.1` landing page remained stable after deploy; `Check` remains visible
because reviewed generated checkpoint output exists.

## Validation Summary

Green checks:

- focused exit-ticket Jest passed with 13/13 tests;
- deploy passed with 472 checked references and 221 data tests;
- complete student-web validation passed for `1.1.1`;
- Book 1 health passed with 26/26 checks;
- procedure-contract validation passed with 341 checks;
- v5 target-exercise validator passed with 54 records and 12/12/14/16 counts;
- full platform Jest passed with 561 tests and 8 skipped.

## Remaining Product Truth

The metadata mismatch is fixed for the current checkpoint role, but the
checkpoint is still not target-exercise-readiness evidence. `A43` remains part
of the `1.1.1` target-exercise skill set and is not yet covered by the
checkpoint's task chain.

That work remains for `L1.7B-Q2`.

## Boundaries

L1.7B-MAP does not authorize:

- diagnostics;
- mastery decisions;
- automatic sequencing;
- grading or summative use;
- student-facing AI;
- PV projection;
- PV machine promotion;
- target-exercise promotion;
- CP-6 or Year-1 closure;
- Scale Gate 1;
- broad companion scaling.
