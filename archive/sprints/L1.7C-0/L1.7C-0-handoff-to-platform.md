# L1.7C-0 Handoff To Platform

Date: 2026-05-23
Status: READY FOR GAME-UX-1

## Handoff Summary

Lesson L1.7C-0 closed the shared skill-map contract needed before the
three-aspect game upgrade. Platform support remains required under
`GAME-UX-1`.

Lesson contract path:

- `archive/sprints/L1.7C-0/L1.7C-0-shared-skill-map-contract.md`

Supporting audit:

- `archive/sprints/L1.7C-0/L1.7C-0-current-state-audit.md`

## Platform Work Requested

Implement or refactor one shared skill-map engine consumed by:

- `Redeneren`
- `Rekenen`
- `Grafieken`
- the later exit-ticket MVP in compact checkpoint mode
- landing-page route previews

Minimum platform requirements:

- expose MTU `aspects` in skill-tree base data;
- support aspect filters `reasoning`, `calculation`, `graphical`, and explicit
  `mixed`;
- support display modes `compact`, `route`, and restricted `full`;
- ensure `full` / all-skill catalog view is never the default student route;
- provide a request/response view model usable by all three practice engines;
- keep stars/progress as local practice progress only;
- add tests for aspect filtering, compact mode, route mode, and full-view
  restrictions;
- add integration tests showing each game requests a scoped route;
- keep all product-use boundary flags false.

## Current Platform Findings

- Existing skill-tree modes are `paragraph`, `chapter`, and `module`.
- `module` is effectively the current all-skill catalog.
- `shared/skilltree/1.1.1.js` currently uses `activeSkills: null`, causing all
  interactive A-domain skills to be visible.
- Existing MTU records already carry aspect labels:
  - `verbaal`
  - `rekenen`
  - `grafisch`
- The generated skill base does not currently expose those aspects to the
  engine contract.

## Boundaries

This handoff does not authorize:

- protected reference mutation;
- machine-reference mutation;
- unit minting;
- lesson-output mutation;
- exit-ticket prototype import;
- target-exercise promotion;
- diagnostics;
- adaptive routing;
- mastery/sequencing;
- student-facing AI;
- summative use;
- PV projection;
- PV machine promotion.

## Lesson-Side Next Step

L1.7C can start as a game-row quality-upgrade sprint using the accepted
contract. If real game-row inspection requires platform implementation first,
L1.7C must pause and wait for `GAME-UX-1`.

