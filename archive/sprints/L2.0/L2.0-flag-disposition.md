# L2.0 Flag Disposition

Date: 2026-05-25
Status: DRAFT FOR HUMAN REVIEW

## Disposition Categories

| Category | Meaning |
|---|---|
| `fix_before_scale` | Must be fixed before broad companion or game scaling. May be acceptable in a controlled pilot. |
| `carry_into_controlled_production` | Can remain visible in controlled production if named in review/quality records and bounded by clear language. |
| `defer` | Not required for the next scale gate; keep as later improvement or future-engine work. |
| `closed` | Resolved by prior sprint evidence. |

## Current Flags

| Flag | Source | Disposition | Rationale |
|---|---|---|---|
| Core graph/table surfaces lacked visible learning objects | L1.6R incident | closed | L1.6R added dual-coding objects and guided visual-value concordance. |
| `1.1.3` Part A figure sequence was `1 -> 3 -> 2` | L-CP6E / CP.6f | closed | L-CP6E regenerated Part A; CP.6f cleared the focused blocker. |
| Repeated worked example in `1.1.3` `opgaven.md` | L-CP6E / CP.6f | carry_into_controlled_production | Accepted as standalone exercise scaffolding; do not hide it, but it is not a scale blocker. |
| `Rekenen / stappenplan` is not a full numeric calculation engine | L1.7C/L1.7D | fix_before_scale | The label is acceptable for pilot use, but full calculation coverage needs calculation input, answer input, unit handling, and feedback. |
| `Grafieken` has only one less-labelled variant | L1.7C | fix_before_scale for graph-heavy scaling | MVP proves the route; graph-heavy paragraphs need more independent axis/grid reading. |
| `Redeneren` needs richer variants and replay value | L1.7C | fix_before_scale for broad game scaling | Current practice is acceptable for controlled pilot use, but not enough as the reusable game standard. |
| Route-panel internal skill IDs | L1.7C first review | closed | Focused revision renders student-facing labels and regression-tested `A61` leakage. |
| `Check` hidden until exit ticket exists | L1.7D | carry_into_controlled_production | This is the correct boundary until L1.7B-R/GATE-L1.7B accepts a checkpoint MVP. |
| Exit-ticket prototype untracked and semantically unsafe | L1.7B-C | fix_before_scale | Must be source-controlled/rebuilt and stripped of mastery/pass/score/adaptive-focus language before use. |
| Screenshot evidence representative rather than exhaustive | L1.7D | carry_into_controlled_production | Fine for the pilot; scale gates need route-pattern sampling. |
| `1.1.4` Verdiep-only landing | L1.7D | fix_before_scale in L2.0 | Consolidation pages need a primary mixed-practice route. |
| Legacy consolidation quality-ref shape | `1.1.4-quality-ref.yaml` | defer | Do not hand-upgrade evidence. Future consolidation quality-ref schema can be handled by a targeted quality workflow. |
| Platform landing-generator old comment/variable remnants | L1.7D | defer | Cleanup-only unless confusing implementation; not student-facing. |

## Scale-Gate Summary

Scale Gate 1 should treat these as still open:

- exit-ticket MVP and boundary review;
- full numeric calculation-engine decision;
- richer graph and reasoning game variants for broad game scaling;
- screenshot QA route-pattern sampling;
- consolidation landing rule proof.

Scale Gate 1 should not reopen:

- L1.6R dual-coding incident;
- L-CP6E figure-order blocker;
- L1.7C internal-ID route-panel leak.
