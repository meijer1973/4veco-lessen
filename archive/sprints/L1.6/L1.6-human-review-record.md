# L1.6 Human Review Record

Date: 2026-05-18
Status: CLOSED PASS WITH FLAGS

## Review Input

The human review packet was `L1.6-human-review-packet.md`.

The lead reviewer inspected the technical QA evidence and returned:

`REVISE`

The focused re-review accepted the correction and returned:

`PASS WITH FLAGS`

## Main Finding

The fresh `1.1.3` build is technically strong and close to closure, but the
graphical-game task `line-broodjes-price-350` used descending quantity labels
left-to-right while the paragraph teaches the economics convention:

- price on the vertical axis;
- quantity on the horizontal axis.

Because the graphical UI positions line-chart points by series order, the data
could visually encode quantity backwards. This was closure-blocking for a
paragraph about graph/table reading.

## Secondary Finding

Presentation speaker notes contained a repository/meta phrase:

`Noem geen interne codes; dit is gewoon de vaardigheid tabelwaarden selecteren.`

This was not the hard blocker, but it needed polish.

## Boundary Review

Boundary verdict: PASS.

No PV machine promotion, student-facing PV projection, adaptive diagnostics,
mastery routing, sequencing, AI use, or summative use was introduced. The
A61/table-trace link remains an internal procedure-contract validation anchor.

## Implementation Response

The revise blockers were addressed through platform source and regenerated
lesson output:

- reordered `Broodjesverkoop` from `100` to `300` broodjes left-to-right;
- added a graphical-data test that checks quantity line charts increase
  left-to-right;
- rephrased the speaker note to ordinary teacher guidance;
- regenerated `1.1.3` student-web outputs, presentation, graphical data, and
  quality-ref;
- reran focused and broad validation.

## Evidence After Revision

| Check | Result |
| --- | --- |
| `node build-scripts/content/book-1/b1-113-student-web.js` | PASS |
| `node build-scripts/content/book-1/b1-113-graphical-data.js` | PASS |
| `node scripts/deploy.js "..\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod"` | PASS; links valid; 217 data tests passed |
| Focused graphical/adaptive Jest | PASS; 30 tests |
| `validate-paragraph --mode complete --profile student-web` for `1.1.3` | PASS with one non-blocking warning |
| `validate-paragraph --mode part-a --profile publisher-print` for `1.1.3` | PASS |
| `validate-procedure-contracts.js` | PASS; 341 checks |
| `check:book` | PASS; 26/26 |
| Full platform Jest | PASS; 510 passed, 8 skipped |
| Presentation screenshot QA | PASS |
| Student-web screenshot QA | PASS |

## Decision

The initial review remains recorded as REVISE, and the focused re-review
accepted the correction. L1.6 closes as PASS WITH FLAGS.

Closure flags:

- The graphical game is still an MVP with scaffolded graph values.
- Harder graph-reading variants remain later work.
- The `1.1.3` v5 target-exercise record remains migrated and not
  final-reviewed.
- Existing Part A flags remain visible and non-blocking.
