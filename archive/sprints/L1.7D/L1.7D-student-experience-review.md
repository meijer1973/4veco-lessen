# L1.7D Student-Experience Review

Date: 2026-05-24
Status: CLOSED PASS WITH FLAGS

## Verdict

Student-experience verdict: **PASS WITH FLAGS**.

The reviewed pilot landing pages now behave like a controlled learning route
rather than a peer-level card dump. `Start`, `Leer`, `Oefen`, and `Verdiep` are
visible and understandable; `Check` is correctly absent while no reviewed
exit-ticket surface exists.

## Review Answers

| Question | Answer |
|---|---|
| Is it clear what to do first? | PASS. The pilot pages place `Start` first and show a clear route menu. |
| Do `Start`, `Leer`, `Oefen`, and `Verdiep` make sense? | PASS. The labels are student-readable and create a better order than the previous peer-level grouping. |
| Does the page avoid feeling like a card dump? | PASS WITH FLAGS. The first view is route-oriented; secondary materials are pushed into `Verdiep`. |
| Is `Oefen` understandable? | PASS. It separates guided support from practice routes. |
| Does `1.1.1` avoid promising a missing graph route? | PASS. It describes redeneren and rekenen only. |
| Does `1.1.3` show all three practice routes? | PASS. It shows `Redeneren`, `Rekenen / stappenplan`, and `Grafieken`. |
| Are secondary surfaces findable without crowding? | PASS WITH FLAGS. Summary, news, presentation, textbook, and full skill map are findable under `Verdiep`; this should be rechecked as more artifact variants appear. |
| Is the absence of `Check` acceptable? | PASS WITH FLAGS. Hiding `Check` is correct. Future exit-ticket wording should distinguish primary `Check` from informal quiz/check copy. |
| Do screenshots show readable route structure? | PASS WITH FLAGS. Representative desktop/mobile light/dark evidence exists for `1.1.1` and `1.1.3`; this is acceptable for L1.7D. |
| What remains confusing? | No closure blocker. Scaling flags remain for calculation-engine expectations and consolidation-page route semantics. |

## Carry-Forward Flags

- `Rekenen / stappenplan` is not yet a true numeric calculation engine.
- Future exit-ticket work must keep `Check` non-summative and avoid mastery or
  diagnostic wording.
- Screenshot QA is representative, not exhaustive.
- `1.1.4 Gemengde opgaven` currently has weak `Verdiep`-only route semantics;
  consolidation pages need a dedicated route pattern before broad scaling.

These are scaling flags, not L1.7D closure blockers.
