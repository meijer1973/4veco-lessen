# 2.1.2-review.md - repository sprint disposition

## Verdict

PASS WITH FLAGS.

This paragraph has been integrated into the repository through sprint
`B2-2.1.2-HARDEN`. The root golden package was imported into the canonical
paragraph folder, rendered output was rebuilt, and lead review round 1 requested
corrections. The duplicate assembled-chapter worked example and the graph
production gap in Opgave 8 have been corrected. Lead review round 2 closed
`PASS WITH FLAGS`.

## Repository checks and corrections

- `paragraaf.md` follows the Book 1 textbook pattern: target exercise, short
  theory blocks, canonical procedure, worked example, summary and recovery
  route.
- `opgaven.md` contains the worked example and an exercise ladder: start,
  guided table, graph reading, independent practice, repetition,
  target-equivalent source task and stretch item.
- `antwoorden.md` mirrors all exercise labels and gives formula, substitution,
  result, unit and economic conclusion where needed.
- Opgave 8 now requires students to draw a `TK`-`TO` graph, mark the
  break-even point, and show the profit at `Q = 400` as the vertical distance
  between `TO` and `TK`.
- The answer model now names the graph axes, line points, break-even point
  `(300, 750)`, and the vertical profit distance at `Q = 400`.
- Chapter assembly now appends only the `## Opgaven` block from `opgaven.md`
  when the paragraph already contains the worked example, preventing duplicate
  worked-example exposure in the combined chapter.
- Figure 1 functions as a source-to-formula schema and avoids decorative
  duplication.
- Figure 2 uses a clean `TK`-`TO` graph. It marks break-even and the vertical
  profit gap at `Q = 1000` without turning the graph into a table of point
  labels.
- The graph explicitly prevents the common area-misreading: profit at a chosen
  `Q` is vertical distance `TO - TK`, not the whole area between lines over a
  range.
- The target operation chain is covered in explanation, worked example,
  exercises and answers.

## Carry-forward flags

- Upstream target exercise record remains
  `migrated_from_v4_needs_v5_review`.
- Lead review round 2 closed `PASS WITH FLAGS`.
- Chapter validation may still warn on Chapter 2.1 PDF size while asset checks
  pass.
