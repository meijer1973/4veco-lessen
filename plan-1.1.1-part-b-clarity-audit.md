# Clarity audit — Part B pipeline for paragraph 1.1.1 (re-run)

Run date: 2026-04-23 (second pass, after bootstrap work).

## Context

You asked to re-run the clarity audit of the Part B pipeline for paragraph **1.1.1 "Schaarste en economisch denken"** against the current state of `4veco-platform/` and `4veco-lessen/`. The first pass identified five bootstrap gaps and one spec-level ambiguity. This re-run verifies each of those against the code and docs as they stand now, plus executes the three end-to-end verification steps from the previous audit.

## Verdict

**The pipeline is now unblocked for 1.1.1.** Every bootstrap gap identified in the previous audit has been addressed in `4veco-platform/BUILD-PARAGRAPH.md` §B0a and in the file system. The spec-level ordering ambiguity (Phase 3 procedure data vs. vaardigheden) has been fixed: a new **Phase 2a** sits before Phase 3, and Phase 3 step 5 now explicitly says the procedure array must be copied from `references/machine/micro-teaching-units.json`, not re-authored.

What remains is normal Part B content work for 1.1.1 (write the paragraph plan, author game data, run the eight scripted-manual builders), not pipeline clarity. `validate-paragraph.js --mode part-b` currently reports **3/24 companion files present** and **26 errors** — expected for a paragraph whose Part B work has not started.

## Verification steps (re-executed)

Run from `4veco-platform/`.

| # | Command | Result | Interpretation |
|---|---------|--------|----------------|
| 1 | `node scripts/deploy.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"` | **Succeeds.** Copies engines, generates `1.1.1 … wiskundevaardigheden.html` and chapter/book/paragraph `index.html`, 55/55 data tests pass, 12 link-check warnings about Part A html's not yet linked from chapter index. Exit: "Deploy complete." | deploy.js is no longer a no-op probe; it **writes** into the target book. BUILD-PARAGRAPH.md §B0a now explicitly warns about this. |
| 2 | `node scripts/validate-paragraph.js "<1.1.1 folder>" --mode part-b` | **FAIL: 26 error(s).** 3/24 files present (`wiskundevaardigheden.html`, `index.html`, `Lees dit…docx`); 21 companion files missing; `_paragraph-plan.md` missing; `shared/{questions,newsdetective,reasoning,procedure}/1.1.1.js` missing; `shared/skilltree/1.1.1.js` present. | Validator behaves exactly as §B1 specifies. Expected fail — Part B has not been built yet. |
| 3 | `grep "Phase 3" BUILD-PARAGRAPH.md` + check for old "MUST align with the vaardigheden skills" wording | Old wording is **gone**. Line 372 now reads: *"After Phase 2a is complete, write `$BOOK/shared/procedure/X.Y.Z.js` — procedure/stappenplan data. Do **not** re-author the procedure ad hoc. For each skill unit in the paragraph plan, copy its canonical `procedure` array from `references/machine/micro-teaching-units.json` into the procedure data file."* | Spec-level ordering ambiguity resolved. |

## Previous-audit findings — status update

### What was "clear and verified" — still holds

All reference scripts, libraries, templates, converters, and platform generators remain in place at the paths documented in §B4. `deploy.js`, `validate-paragraph.js`, `lib-deploy-config.js`, `template-paragraph-plan.md`, and `references/machine/micro-teaching-units.json` unchanged in role.

### Bootstrap gaps — all resolved

| # | Previous gap | Current state |
|---|--------------|---------------|
| 1 | Boek 1 had no `deploy-config.json` | `deploy-config.json` exists at `4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod/deploy-config.json` (823 B). Registers chapter 1.1 and all four paragraphs (1.1.1 … 1.1.4) under domain `teal`. 1.1.1 declares `skilltree: { skills: null }` (addresses previous gap #5 — all math skills shown). |
| 2 | Boek 1 had no `shared/` folder | `shared/` exists with all five required subdirs (`questions/`, `reasoning/`, `newsdetective/`, `procedure/`, `skilltree/`), each with `.gitkeep`. Engine JS/CSS is copied in by `deploy.js`. `shared/skilltree/1.1.1.js` (0.5 KB) generated at deploy time. |
| 3 | Platform lacked `build-scripts/content/book-1/` and `source-data/book-1/reasoning/` | Both directories exist with README.md placeholders. READMEs document naming conventions (`b1-111-presentatie.js`, `1.1.1.csv`). |
| 4 | Static "Lees dit…" docx source path was undocumented | BUILD-PARAGRAPH.md §B0a (lines 145–147) now gives the exact copy path (`3-Module-3-rewire-test/…/1. Voorbereiden/…docx`). The file is already present in 1.1.1's folder (14.5 KB). |
| 5 | Skilltree skill selection for 1.1.1 was undecided | Manifest declares `skills: null` → all math skills visible. wiskundevaardigheden.html has been generated by deploy.js. Decision is made and live. |

### Spec-level ambiguity (finding 6) — resolved

Phase structure was refactored. The workflow is now:

- **Phase 1** — Register in manifest
- **Phase 2** — Create folder structure
- **Phase 2a (new)** — Create paragraph plan (15 min) from `template-paragraph-plan.md`
- **Phase 3** — Create game data files; procedure data explicitly pulled from `micro-teaching-units.json` after Phase 2a
- **Phase 4** — Rich documents
- **Phase 5** — docx → html conversions
- **Phase 5a–5c** — QC gates
- **Phase 6** — Deploy

The paragraph plan is now the first real artifact produced, and it is the input to both Phase 3 (game data) and Phase 4 (rich documents). Ordering is coherent.

### New clarity item surfaced by the re-run

**§B0a explicitly warns**: *"`scripts/deploy.js` is **not** a read-only probe. It writes engine files and generated shells into the target book. Do not use deploy as a harmless existence check."* This directly invalidates part of the previous audit's Verification section, which treated `deploy.js` as a non-destructive probe. With the manifest present, a deploy run materialises engines in `<book>/shared/`, generates skilltree shells, and rebuilds all `index.html` files. The warning in the docs is appropriate.

## Remaining work to produce 1.1.1 Part B

This is content work, not pipeline clarity. Listed here so the re-run is actionable.

1. **Phase 2a — write `_paragraph-plan.md`** for 1.1.1 from `build-scripts/templates/template-paragraph-plan.md`.
2. **Phase 3 — author game data files** for 1.1.1:
   - `shared/questions/1.1.1.js` (quiz, 15 Q)
   - `source-data/book-1/reasoning/1.1.1.csv` → generates `shared/reasoning/1.1.1.js`
   - `shared/newsdetective/1.1.1.js`
   - `shared/procedure/1.1.1.js` — copy from `micro-teaching-units.json` per the Phase 3 step 5 rule
3. **Phase 4 — run the eight scripted-manual builders** (voorkennis, vaardigheden, presentatie, nieuws, samenvatting, inoefening, opgavensets ×3 levels, plus manual youtube-videos.html). Save each adapted script under `build-scripts/content/book-1/` per §B8.
4. **Phase 5** — run the three docx→html converters.
5. **Phase 5a–5c** — QC gates (asset completeness, didactic review, quality_ref).
6. **Phase 6** — `node scripts/deploy.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"`. Then confirm `validate-paragraph.js --mode part-b` reports 0 errors.

## Critical files referenced

- `C:\Projects\4veco\4veco-platform\BUILD-PARAGRAPH.md` (primary spec; §B0a now covers bootstrap; §B3 Phase 2a inserted before Phase 3)
- `C:\Projects\4veco\4veco-platform\scripts\deploy.js`
- `C:\Projects\4veco\4veco-platform\scripts\validate-paragraph.js`
- `C:\Projects\4veco\4veco-platform\build-scripts\lib\lib-deploy-config.js`
- `C:\Projects\4veco\4veco-platform\build-scripts\templates\template-paragraph-plan.md`
- `C:\Projects\4veco\4veco-platform\references\machine\micro-teaching-units.json`
- `C:\Projects\4veco\4veco-platform\build-scripts\content\book-1\README.md`
- `C:\Projects\4veco\4veco-platform\source-data\book-1\reasoning\README.md`
- `C:\Projects\4veco\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod\deploy-config.json`
- `C:\Projects\4veco\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod\1.1 Hoofdstuk Economisch denken en rekenen\1.1.1 Schaarste en economisch denken\` (target; 3/24 Part B files present + static docx)

## Summary

| Item | First-pass finding | Re-run status |
|------|--------------------|---------------|
| Reference scripts exist | Clear | Still clear |
| `deploy.js` book-agnostic | Clear | Still clear |
| `validate-paragraph.js` 4 modes | Clear | Still clear |
| Bootstrap 1 — deploy-config.json | Missing | Present |
| Bootstrap 2 — book shared/ | Missing | Present (all 5 subdirs + engines) |
| Bootstrap 3 — platform book-1 dirs | Missing | Present |
| Bootstrap 4 — static "Lees dit…" source path | Undocumented | Documented in §B0a + file seeded |
| Bootstrap 5 — skilltree decision for 1.1.1 | Undecided | `skills: null` (all) |
| Spec ambiguity 6 — Phase 3 vs 4a ordering | Ambiguous | Resolved via new Phase 2a + explicit copy-from-catalog rule |

The pipeline is clear. The remaining work on 1.1.1 is content authoring through Phases 2a → 6, not pipeline clarification.
